import React from 'react';
import NavFrontendSpinner from 'nav-frontend-spinner';

const Lasteboks = () => (
    <div className="spinner">
        <NavFrontendSpinner type="L" />
    </div>
);

export default Lasteboks;
