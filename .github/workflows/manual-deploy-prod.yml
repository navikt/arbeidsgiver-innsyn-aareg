name: Deploy til dev
on:
  workflow_dispatch:


jobs:
  bygg:
    permissions:
      contents: "read"
      id-token: "write"
    name: Bygg
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: nais/login@v0
        id: login
        with:
          team: arbeidsforhold
      - uses: nais/deploy/actions/deploy@v2
        env:
          CLUSTER: prod-gcp
          RESOURCE: nais/prod-gcp.yaml
          VAR: commit=${{ github.sha }},image=${{ steps.login.outputs.registry }}/arbeidsgiver-innsyn-aareg:${{ github.sha }}
          REF: ${{ github.sha }}