/// <reference types="react" />
export interface HTTPError {
    code: string;
    text: string;
}
interface Props {
    error: HTTPError;
    locale: string;
}
declare const Error: (props: Props) => JSX.Element;
export default Error;
