/// <reference types="react" />
import { AFArbeidsgiver } from "../../types/arbeidsforhold";
interface Props {
    arbeidsgiver: AFArbeidsgiver;
    overskrift?: boolean;
}
declare const ArbeidsgiverTittel: (props: Props) => JSX.Element | null;
export default ArbeidsgiverTittel;
